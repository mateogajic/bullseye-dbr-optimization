# Local simulation data

Place the RSoft spatial-monitor files required by the scripts in this directory.
The raw data are intentionally excluded from version control by `.gitignore`.

See the repository `README.md` and the configuration section of each script for
the expected directory structure and filename convention.
